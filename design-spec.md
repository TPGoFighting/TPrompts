# TPrompts 提示词模板网站 — 设计思路与规范文档

> 版本：v1.0 ｜ 参考模板：`E:\B_GAME\ex\TPrompts\03-prompt-pop.html`（新粗野主义/波普风）
> 交付物：`v1-minimal.html`（极简清新）/ `v2-pop.html`（活力撞色）/ `v3-terminal.html`（暗夜极客）

---

## 1. 产品定位与信息架构

### 1.1 定位
面向 AI 重度用户的「提示词资产库」：不是什么都收录的大杂烩，而是**经过筛选、可直接复制使用**的精品模板站。核心体验闭环：**发现 → 筛选 → 复制 → 使用**。

### 1.2 信息架构（IA）

```
TPrompts
├── 首页（Home）
│   ├── Hero：品牌主张 + 全局搜索
│   ├── 提示词库：分类筛选（标签 chips + 关键词搜索）+ 卡片流
│   ├── 灵感板块：每日推荐（按日期轮换）+ 热门榜单 + 创意用法
│   ├── 品味板块：编辑精选合集（Collections）
│   └── 页脚：Roadmap / 投稿入口 / 关于
├── （预留）分类页 /categories
├── （预留）提示词详情页 /prompt/:id
├── （预留）合集详情页 /collection/:id
└── （预留）投稿页 /submit
```

**扩展性原则**：当前为单页实现，但导航、路由锚点（`#/explore`、`#/collections`）和数据结构均已按多页应用预留，后续可平滑迁移到 hash 路由或任意前端框架。

### 1.3 数据模型（为迭代铺垫的核心）

所有版本共用同一份数据 schema，**新增提示词 = 在 `PROMPTS` 数组里加一条记录**，页面自动渲染、自动进入筛选与搜索：

```js
{
  id: "p01",               // 唯一 ID，预留详情页锚点
  title: "标题",
  desc: "一句话说明（卡片展示）",
  prompt: "完整提示词正文（复制的内容）",
  category: "写作",         // 一级分类
  tags: ["文案", "小红书"],  // 自由标签，驱动筛选 chips
  hot: 98,                 // 热度值，驱动热门榜单排序
  featured: true,          // 是否进入「品味」精选
  useCase: "创意用法说明",   // 灵感板块素材
  addedAt: "2026-08-18"    // 预留：按时间排序 /「最新」标签
}
```

合集（品味板块）schema：

```js
{ id: "c01", title, desc, promptIds: ["p01","p03"], curator: "编辑名", coverTone: "coral" }
```

> 后续迭代路径：数据数组 → JSON 文件 → CMS/Notion API → 用户投稿后台，前端渲染逻辑零改动。

---

## 2. 页面布局方案

### 2.1 栅格与容器
- 内容容器：`width: min(1160px, 100% - 32px)`，水平居中
- 卡片栅格：桌面 3 列（`repeat(3, 1fr)`，gap 14–20px）→ 平板 2 列（≤1024px）→ 手机 1 列（≤640px）
- 板块垂直节奏：section padding `64px 0`，板块标题与内容间距 24px

### 2.2 首页区块顺序
| 顺序 | 区块 | 职责 |
|---|---|---|
| 1 | Sticky 导航 | 品牌 + 锚点导航 + 投稿按钮（预留） |
| 2 | Hero | 大标题主张 + 搜索框（关键词直达卡片流） |
| 3 | 提示词库 | 分类 chips + 卡片网格（核心功能区，首屏可达） |
| 4 | 灵感板块 | 每日推荐大卡 + 热门 Top5 榜单 + 创意用法 |
| 5 | 品味板块 | 编辑精选合集卡片（2 列大卡） |
| 6 | 页脚 | Roadmap（透明化迭代计划）+ 版权 |

### 2.3 卡片解剖
```
┌─────────────────────────┐
│ 序号/分类      [热度标记] │
│ 标题（2行截断）           │
│ 描述（3行截断）           │
│                         │
│ #标签 #标签    [复制 ⧉]  │ ← 底部固定
└─────────────────────────┘
```
- 卡片为 flex column，底部操作区 `margin-top: auto` 对齐，高度不一也整齐
- 整张卡片预留点击（→ 详情页），复制按钮 `stopPropagation`

---

## 3. 配色系统（三套主题）

### 3.1 V1 极简清新 `v1-minimal.html`
| Token | 值 | 用途 |
|---|---|---|
| `--bg` | `#FAFAF8` | 米白底 |
| `--surface` | `#FFFFFF` | 卡片 |
| `--ink` | `#1A1A1A` | 主文字 |
| `--muted` | `#8A8A85` | 次要文字 |
| `--accent` | `#2F6B4F` | 主强调（墨绿） |
| `--accent-soft` | `#E7F0EA` | 标签/高亮底 |
| `--line` | `#E6E4DE` | 细分割线 |

气质：编辑杂志感、大量留白、单强调色，靠字重和间距分层。

### 3.2 V2 活力撞色 `v2-pop.html`（继承参考模板基因）
| Token | 值 | 用途 |
|---|---|---|
| `--cream` | `#FFF9F1` | 奶油底 |
| `--ink` | `#1F1B19` | 墨色（边框/文字/硬阴影） |
| `--coral` | `#FF6B57` | 主强调 |
| `--lime` | `#D9EF7F` | 标签/高亮 |
| `--sky` | `#A8DDFF` | 卡片轮换色 |
| `--pink` | `#FFC5D8` | 跑马灯/卡片轮换色 |

气质：新粗野主义波普 —— 2px 墨线描边、无模糊硬阴影（`4px 4px 0`）、撞色块、跑马灯。

### 3.3 V3 暗夜极客 `v3-terminal.html`
| Token | 值 | 用途 |
|---|---|---|
| `--bg` | `#0D1117` | 深空黑 |
| `--surface` | `#161B22` | 卡片 |
| `--line` | `#2A3140` | 边框 |
| `--ink` | `#E6EDF3` | 主文字 |
| `--muted` | `#8B949E` | 次要文字 |
| `--neon` | `#7EE787` | 终端绿（主强调） |
| `--purple` | `#D2A8FF` | 次强调 |
| `--amber` | `#FFA657` | 热度/榜单 |

气质：IDE/终端既视感 —— 等宽字体点缀、霓虹辉光、复制按钮做成 `$ copy` 命令样式。

---

## 4. 字体规范

| 角色 | 字族 | 字重 | 尺寸（桌面） |
|---|---|---|---|
| 展示大标题 | Inter / PingFang SC，V3 用 `"JetBrains Mono", ui-monospace` | 800–900 | `clamp(40px, 7vw, 84px)`，负字距 `-0.03em ~ -0.06em` |
| 板块标题 | 同上 | 800 | 28–34px |
| 卡片标题 | 同上 | 700 | 17–20px |
| 正文/描述 | 同上 | 400–500 | 14–15px / 行高 1.6–1.7 |
| 标签/元信息 | 同上（V3 等宽） | 600–700 | 11–12px，大写 + `letter-spacing .08em` |
| 提示词正文 | `ui-monospace, SFMono-Regular, Menlo` | 400 | 13–14px / 行高 1.7 |

中文回退链统一：`Inter, ui-sans-serif, system-ui, "PingFang SC", "Microsoft YaHei", sans-serif`。**全站无外部字体请求**，保证单文件离线可用。

---

## 5. 组件样式规范

### 5.1 按钮
| 类型 | 规范 |
|---|---|
| 主按钮（复制） | 强调色填充，12px 圆角（V2 为墨线+硬阴影），padding `8px 14px`，字重 700 |
| 次按钮（投稿/导航 CTA） | 描边样式，悬停反色 |
| 状态 | hover：位移或反色；active：下沉；focus-visible：2px 外描边（无障碍） |

### 5.2 标签 Chips（筛选器）
- 圆角胶囊 `999px`，未选中=描边/灰底，选中=强调色实底反白
- 支持「全部」复位项；点击即过滤，配合搜索框做 AND 逻辑
- 选中态计数提示（如 `写作 · 6`）

### 5.3 卡片
- 圆角：V1 `16px` / V2 `18px` / V3 `10px`
- 悬停：V1 上浮 4px + 柔和阴影；V2 反向位移 + 硬阴影加大；V3 边框点亮 + 霓虹辉光
- 过渡统一 `transition: all .18s ease`

### 5.4 搜索框
- 高度 48–52px，左侧放大镜符号（纯字符 ⌕ / `>`，不引图标库）
- 输入即过滤（`input` 事件，无防抖依赖，数据量小直接过）

### 5.5 Toast（复制反馈）
- 固定左下/底部居中，深色胶囊，文案「已复制到剪贴板 ✓」
- 入场：opacity + translateY；1.4s 自动消失
- 同时复制按钮本身短暂变为「已复制 ✓」态（双重反馈）

### 5.6 每日推荐
- 纯前端实现：`(日期序数) % featured.length` 取模轮换，无需后端
- 大卡展示完整提示词预览（等宽字体块）+ 复制按钮

---

## 6. 交互细节

| 交互 | 规范 |
|---|---|
| 卡片悬停 | 位移 + 阴影变化，150–200ms，ease-out |
| 复制反馈 | Clipboard API → 降级 `execCommand`（textarea 方案）；按钮变「✓ 已复制」1.4s + Toast |
| 筛选 | chip 单选 + 关键词实时过滤，空结果显示「无匹配」占位插画文案 |
| 导航 | 锚点平滑滚动（`scroll-behavior: smooth`），sticky 顶栏毛玻璃/实底 |
| 榜单 | hot 值降序取 Top5，显示热度条（宽度 % 可视化） |
| 键盘 | 搜索框 `/` 快捷聚焦（预留，`keydown` 监听） |

---

## 7. 响应式规则

| 断点 | 调整 |
|---|---|
| ≥1025px 桌面 | 卡片 3 列；灵感板块 左右两栏（大卡+榜单） |
| 641–1024px 平板 | 卡片 2 列；Hero 标题降为 `clamp` 中段；导航保留 |
| ≤640px 手机 | 卡片 1 列；导航折叠为底部锚点条或省略次级链接；灵感/品味全部单列；Hero 搜索全宽；Toast 全宽容器内居中 |

其他：触控目标 ≥44px；`prefers-reduced-motion` 时关闭位移动画（预留 media query）。

---

## 8. 迭代预留清单（Roadmap 挂钩）

1. **投稿入口**：导航 CTA 已占位 → 后续接表单/腾讯问卷
2. **详情页**：卡片 `id` 锚点已预留 → hash 路由 `/prompt/:id`
3. **收藏**：localStorage 存 `favorites: [id]`，数据层无侵入
4. **多主题**：三套皮肤共享 DOM 结构与数据 → 可合并为 CSS 变量切换的单一站点
5. **数据外置**：`PROMPTS`/`COLLECTIONS` 数组 → `fetch('prompts.json')`，渲染函数不变
6. **统计**：复制按钮预留 `data-id`，可接埋点统计真实热度，反哺 hot 值

---

## 9. 三版风格速查

| | V1 极简清新 | V2 活力撞色 | V3 暗夜极客 |
|---|---|---|---|
| 气质 | 编辑杂志 / 安静高级 | 波普 / 年轻张扬 | 终端 / 极客硬核 |
| 底色 | 米白 | 奶油 | 深空黑 |
| 强调色 | 墨绿 | 珊瑚橙+撞色组 | 终端绿+霓虹紫 |
| 边框 | 1px 细线 | 2px 墨线+硬阴影 | 1px 暗线+辉光 |
| 适合人群 | 泛用户、内容向 | 年轻创作者 | 开发者/重度 AI 用户 |
