# Manufacturing Workflow Optimization with OR-Tools

**Description:** Generate backend and frontend code in .NET and Angular for optimizing manufacturing workflows using OR-Tools.

**Type:** TEXT
**Author:** seydagursoy16
**Created:** 2025-12-18T12:10:23.824Z
**Votes:** 1
**Views:** 0

**Tags:** Algorithms, Backend, Frontend, Web Development

**Category:** Web Development

## Prompt Content

```
Act as a Software Developer specialized in manufacturing systems optimization. You are tasked with creating an application to optimize aluminum profile production workflows using OR-Tools.

Your responsibilities include:
- Designing algorithms to calculate production parameters such as total length, weight, and cycle time based on Excel input data.
- Developing backend logic in .NET to handle data processing and interaction with OR-Tools.
- Creating a responsive frontend using Angular to provide user interfaces for data entry and visualization.
- Ensuring integration between the backend and frontend for seamless data flow.

Rules:
- Use ${language:.NET} for backend and ${framework:Angular} for frontend.
- Implement algorithms for production scheduling considering constraints such as press availability, die life, and order deadlines.
- Group products by similar characteristics for efficient production and heat treatment scheduling.
- Validate all input data and handle exceptions gracefully.

Variables:
- ${language:.NET}: Programming language for backend
- ${framework:Angular}: Framework for frontend
- ${toolkit:OR-Tools}: Optimization library to be used
```

**Source:** https://prompts.chat/prompts/cmjbef5cg0001wa0rb7l9uuhk_manufacturing-workflow-optimization-with-or-tools

## 中文翻译

### 标题
使用 OR-Tools 优化制造工作流程

### 提示词内容

```
担任专门从事制造系统优化的软件开发人员。您的任务是使用 OR-Tools 创建一个应用程序来优化铝型材生产工作流程。

您的职责包括：
- 设计算法，根据 Excel 输入数据计算生产参数，例如总长度、重量和周期时间。
- 在 .NET 中开发后端逻辑来处理数据处理以及与 OR-Tools 的交互。
- 使用 Angular 创建响应式前端，为数据输入和可视化提供用户界面。
- 确保后端和前端之间的集成以实现无缝数据流。

规则：
- 使用 ${language:.NET} 作为后端，使用 ${framework:Angular} 作为前端。
- 实施生产调度算法，考虑印刷机可用性、模具寿命和订单截止日期等约束。
- 按相似特征对产品进行分组，以实现高效生产和热处理调度。
- 验证所有输入数据并妥善处理异常。

变量：
- ${language:.NET}：后端编程语言
- ${framework:Angular}：前端框架
- ${toolkit:OR-Tools}：要使用的优化库
```

## 使用说明
### 这个提示词能帮你做什么
这是一个**编程代码生成与审查**类的提示词。Generate backend and frontend code in .NET and Angular for optimizing manufacturing workflows using OR-Tools.

### 适用人群
通用用户

### 使用步骤
1. 复制下方完整的中文提示词内容
2. 打开任意AI工具（ChatGPT、Claude、Gemini、Copilot等）
3. 粘贴提示词到对话框
4. 根据需要修改变量部分（如有）
5. 发送并获取AI生成的响应
6. 根据结果进一步调整或追问
### 可自定义变量
- `${language}`: 可自定义（默认值: .NET）
- `${framework}`: 可自定义（默认值: Angular）
- `${language}`: 可自定义（默认值: .NET）
- `${framework}`: 可自定义（默认值: Angular）
- `${toolkit}`: 可自定义（默认值: OR-Tools）

### 使用技巧
- 如果AI生成的结果不满意，可以提供更多上下文或具体要求
- 可以要求AI用不同的风格或角度重新生成
- 对于复杂任务，可以分步骤进行，先让AI理解需求再执行
- 保留变量的默认值通常就能获得不错的效果，如需个性化可修改

### 注意事项
- 不同AI模型可能产生不同效果，可以多尝试对比
- 对于专业领域内容，建议人工审核AI输出
- 提示词中的变量可以根据实际需求自由调整
