# Build a DDQN Snake Game with TensorFlow.js in a Single HTML File

**Description:** Create a Deep Q-Network (DQN) based Snake game using TensorFlow.js with the latest API, implemented in a single HTML file.

**Type:** TEXT
**Author:** niels
**Created:** 2026-01-22T19:35:19.295Z
**Votes:** 1
**Views:** 0

**Tags:** AI Tools, Machine Learning, JavaScript, Web Development, Games

**Category:** Web Development

## Prompt Content

```
Act as a TensorFlow.js expert. You are tasked with building a Deep Q-Network (DDQN) based Snake game using the latest TensorFlow.js API, all within a single HTML file. 

Your task is to:
1. Set up the HTML structure to include TensorFlow.js and other necessary libraries.
2. Implement the Snake game logic using JavaScript, ensuring the game is fully playable.
3. Use a Double DQN approach to train the AI to play the Snake game.
4. Ensure the game can be played and trained directly within a web browser.

You will:
- Use TensorFlow.js's latest API features.
- Implement the game logic and AI in a single, self-contained HTML file.
- Ensure the code is efficient and well-documented.

Rules:
- The entire implementation must be contained within one HTML file.
- Use variables like ${canvasWidth:400}, ${canvasHeight:400} for configurable options.
- Provide comments and documentation within the code to explain the logic and TensorFlow.js usage.
```

**Source:** https://prompts.chat/prompts/cmkpuq51b0007ky047sm3l7q9_build-a-ddqn-snake-game-with-tensorflowjs-in-a-single-html-file

## 中文翻译

### 标题
在单个 HTML 文件中使用 TensorFlow.js 构建 DDQN 贪吃蛇游戏

### 提示词内容

```
充当 TensorFlow.js 专家。您的任务是使用最新的 TensorFlow.js API 构建基于 Deep Q-Network (DDQN) 的贪吃蛇游戏，所有操作都在单个 HTML 文件中。 

你的任务是：
1. 设置 HTML 结构以包含 TensorFlow.js 和其他必要的库。
2. 使用JavaScript实现贪吃蛇游戏逻辑，确保游戏完全可玩。
3. 使用 Double DQN 方法训练 AI 玩贪吃蛇游戏。
4. 确保可以直接在网络浏览器中玩和训练游戏。

您将：
- 使用 TensorFlow.js 的最新 API 功能。
- 在一个独立的 HTML 文件中实现游戏逻辑和 AI。
- 确保代码高效且文档齐全。

规则：
- 整个实现必须包含在一个 HTML 文件中。
- 使用 ${canvasWidth:400}、${canvasHeight:400} 等变量作为可配置选项。
- 在代码中提供注释和文档来解释逻辑和 TensorFlow.js 的用法。
```

## 使用说明
### 这个提示词能帮你做什么
这是一个**编程代码生成与审查**类的提示词。Create a Deep Q-Network (DQN) based Snake game using TensorFlow.js with the latest API, implemented in a single HTML file.

### 适用人群
通用用户

### 使用步骤
1. 复制下方完整的中文提示词内容
2. 打开任意AI工具（ChatGPT、Claude、Gemini、Copilot等）
3. 粘贴提示词到对话框
4. 根据需要修改变量部分（如有）
5. 发送并获取AI生成的响应
6. 根据结果进一步调整或追问
### 可自定义变量
- `${canvasWidth}`: 可自定义（默认值: 400）
- `${canvasHeight}`: 可自定义（默认值: 400）

### 使用技巧
- 如果AI生成的结果不满意，可以提供更多上下文或具体要求
- 可以要求AI用不同的风格或角度重新生成
- 对于复杂任务，可以分步骤进行，先让AI理解需求再执行
- 保留变量的默认值通常就能获得不错的效果，如需个性化可修改

### 注意事项
- 不同AI模型可能产生不同效果，可以多尝试对比
- 对于专业领域内容，建议人工审核AI输出
- 提示词中的变量可以根据实际需求自由调整
